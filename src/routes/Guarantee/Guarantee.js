import React from 'react';
import s from './Guarantee.module.scss';

const Guarantee = () => {
    return (
        <section className={s.guarantee}>Guarantee</section>
    )
};

export default Guarantee;