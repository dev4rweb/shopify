import React from 'react';
import s from './Main.module.scss';

const Main = () => {
    return (
        <section className={s.main}>
            Products
        </section>
    )
};

export default Main;